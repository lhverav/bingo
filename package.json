{
  "name": "bingo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "dev:web": "turbo dev --filter=web-host",
    "dev:mobile": "turbo dev --filter=mobile-player",
    "build:web": "turbo build --filter=web-host",
    "build:mobile": "turbo build --filter=mobile-player"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
